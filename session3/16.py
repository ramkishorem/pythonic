"""
Statistics

There are some built-in Python functions to perform basic 
statistical operations on lists like finding minimum value, 
maximum value, sum.
"""

digits = list(range(15))
print(min(digits)) # Output: 0
print(max(digits)) # Output: 14
print(sum(digits)) # Output: 105
